{
  "name": "artifact-kit",
  "private": true,
  "version": "0.0.0",
  "license": "AGPL-3.0-or-later",
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "devDependencies": {}
}
