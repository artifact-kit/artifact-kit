export default function HomePage() {
  return (
    <main className="empty-state">
      <p className="eyebrow">DeckKit Workbench</p>
      <h1>Open a workbench page</h1>
      <p>Use a page-specific URL such as <code>/bbox-review?id=&lt;session-id&gt;</code>. The MCP server only stores JSON session data.</p>
    </main>
  )
}
