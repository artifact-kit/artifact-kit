# Examples

Source example assets are kept in named folders, for example:

- `iot-innovation-project/source.png`

Generated reconstruction workspaces should use a `*-work` suffix, such as `iot-innovation-project-bbox-work`.
Those folders are ignored by git so crop, compare, overlay, and iteration artifacts stay local.
